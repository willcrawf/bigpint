import React from 'react'

const LoginSpotify = (user) => {
    return (
    <a href="http://localhost:8888">
        <button>Login to Spotify</button>
    </a>
      );
}
 
export default LoginSpotify;